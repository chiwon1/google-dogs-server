extends layout

block content
  h1= "Google Dogs"
  if username
    form(action="/logout" method="post")
      button= `${username} Logout`
  else
    form(action="/login" method="get")
      button= "Login"
